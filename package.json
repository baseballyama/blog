{
  "name": "blog",
  "type": "module",
  "scripts": {
    "dev": "node scripts/build.ts --watch & node scripts/serve.ts",
    "build": "node scripts/build.ts",
    "serve": "node scripts/serve.ts"
  },
  "engines": {
    "node": ">= 24"
  },
  "dependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "@types/node": "^24.0.0",
    "marked": "^17.0.0"
  }
}
