{
	"name": "blog",
	"version": "1.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "node scripts/build-head-inline.mjs && vite dev",
		"build:assets": "node scripts/fetch-stars.mjs && node scripts/build-ogp.mjs && node scripts/build-avatar.mjs && node scripts/build-mermaid.mjs && node scripts/build-head-inline.mjs",
		"build": "pnpm build:assets && vite build",
		"preview": "vite preview",
		"preview:workers": "wrangler dev",
		"deploy": "wrangler deploy",
		"cf-typegen": "wrangler types",
		"prepare": "svelte-kit sync",
		"check": "svelte-kit sync && rsvelte-check --tsgo --incremental --tsconfig ./tsconfig.json",
		"check:legacy": "svelte-kit sync && rsvelte-check --tsconfig ./tsconfig.json",
		"check:watch": "svelte-kit sync && rsvelte-check --tsgo --incremental --tsconfig ./tsconfig.json --watch",
		"lint": "oxlint && rsvelte-lint src/",
		"lint:fix": "oxlint --fix && rsvelte-lint --fix src/",
		"lint:ci": "oxlint --deny-warnings && rsvelte-lint --max-warnings 0 src/",
		"format": "rsvelte-fmt",
		"format:check": "rsvelte-fmt --check"
	},
	"dependencies": {
		"marked": "^18.0.0",
		"mermaid": "^11.16.0"
	},
	"devDependencies": {
		"@fontsource-variable/jetbrains-mono": "^5.3.0",
		"@resvg/resvg-js": "^2.6.2",
		"@rsvelte/fmt": "^0.7.0",
		"@rsvelte/lint": "^0.9.0",
		"@rsvelte/svelte-check": "^0.5.0",
		"@sveltejs/adapter-cloudflare": "^7.2.9",
		"@sveltejs/kit": "^2.61.1",
		"@sveltejs/vite-plugin-svelte": "^7.1.2",
		"@types/node": "^25.9.1",
		"@typescript/native-preview": "7.0.0-dev.20260707.2",
		"oxfmt": "^0.60.0",
		"oxlint": "^1.67.0",
		"sharp": "^0.35.3",
		"svelte": "^5.56.0",
		"typescript": "^6.0.0",
		"vite": "^8.0.0",
		"wrangler": "^4.114.0"
	},
	"engines": {
		"node": ">= 22.12"
	},
	"packageManager": "pnpm@11.17.0"
}
